import { StyleSheet } from 'react-native';

export const screen = {
	style: StyleSheet.create({
		container: {
			flex: 1,
			backgroundColor: '#fff',
			alignItems: 'center',
			justifyContent: 'center',
		},
	}),
};

export default {
	screen,
};
